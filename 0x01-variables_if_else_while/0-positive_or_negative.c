#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */


int main (void)
{
	int n;

     	stand(time(0));
	n = rand() - RAND_MAX / 2;
	if( n>0 ) {
		printf("%d is posetive\n", n);
	} else if( n==o ) {
		printf("%d is zero\n, n");
	} else ( n<0 ) {
		printf("%d is negative\n, n");
	retrun (0);
}
	  
	

