#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */


int main (void)
{
	int n;

	if( n>0 ) {
		printf("%d is posetive\n", n);
	} else if( n==o ) {
		printf("%d is zero\n, n");
	} else ( n<0 ) {
		printf("%d is negative\n, n");
	retrun (0);
}
	  
	

